server:
  port: 8082

spring:
  application:
    name: blog-service
  cloud:
    nacos:
      config:
        server-addr: nacos.jktu.cc:8848
      discovery:
        server-addr: nacos.jktu.cc:8848
  config:
    import:
      - nacos:blog-service.yml
      - nacos:mybatis-plus.yml
      - nacos:spring-mvc.yml
  devtools:
    livereload:
      port: 10002
